# MEPA CRM API

Backend técnico Laravel da fundação P0. Expõe actualmente apenas `GET /api/v1/health`; módulos de negócio e autenticação definitiva não fazem parte desta etapa.

Consulte o `README.md` da raiz e `mepa_crm_v1.0.1.md` antes de alterar esta aplicação.

