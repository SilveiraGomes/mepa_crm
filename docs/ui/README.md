# Fundação de interface

A interface nasce mobile-first, em português de Angola (`pt-AO`), com acessibilidade, componentes consistentes e estados claros de loading, vazio, erro e sucesso. Formulários extensos serão multi-etapas.

A presente etapa contém apenas o ecrã técnico de fundação; não inclui Dashboard funcional.

