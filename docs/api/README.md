# API

A API REST usa o prefixo `/api/v1`. Nesta fundação existe apenas `GET /api/v1/health`, sem dependência de base de dados.

Contratos funcionais, paginação, erros e idempotência serão detalhados numa etapa P0 posterior antes dos módulos de negócio.

