# P0.3.2 — Gate da Wave 2

**Base:** `P0.3.2_wave2_audit.md` (auto-auditoria — ver aviso de independência no topo desse
documento) e `P0.3.2_wave2_implementation.md`.

## Critérios do gate (conforme definidos no pedido original de auditoria)

| Critério | Estado |
|---|---|
| 0 achados CRITICAL | ✅ Cumprido (zero) |
| 0 achados HIGH pendentes | ✅ Cumprido (zero) |
| W1-F01 materializado | ✅ Confirmado fisicamente, `RESTRICT`/`RESTRICT`, com teste de regressão negativo |
| Concorrência aprovada | ✅ 2/10/30 processos reais, 0 duplicados, serialização e idempotência provadas |
| fresh + upgrade + rollback + remigrate PASS | ✅ Ambos os caminhos executados e verificados via `information_schema` |
| Drift inesperado = 0 | ✅ `validate-wave2-schema.cjs` físico e estático: `STRICT_PARITY_PASS`, 0 erros |
| Histórico preservado (transferência/ministério) | ✅ Confirmado — nenhuma linha apagada, apenas fechada/aberta |
| SEM NOMEAÇÃO representável | ✅ Posto vago sem Person/string fictícia, comprovado |
| Graphify coerente | ✅ Encolhimento investigado e explicado (deduplicação de conteúdo pré-existente, não perda da Wave 2); saúde do grafo OK |
| D-02–D-12 intactos | ✅ `git diff` vazio sobre `docs/database/` |

Todos os critérios técnicos definidos para o gate estão cumpridos. Os quatro achados (secção 19
da auditoria) são, no máximo, MEDIUM e não bloqueiam tecnicamente a Wave 3.

## Decisão

**CONDICIONALMENTE APROVADA**, seguindo exactamente o precedente já estabelecido pelo próprio
gate da Wave 1 (que aprovou a Wave 2 com quatro condições a fechar *durante* a wave seguinte, não
antes de a iniciar): as condições abaixo devem ser fechadas durante a Wave 3, não bloqueiam o seu
início.

### Condições a fechar durante a Wave 3

1. **Auditoria independente (padrão P0.2 → P0.2-F).** Esta auditoria não é independente — foi
   feita pela mesma parte que implementou a Wave 2, com o conhecimento e aceitação explícita do
   utilizador. Recomenda-se uma reconciliação independente (P0.3.2-F) antes do fecho institucional
   definitivo da Wave 2, mesmo que a Wave 3 já tenha arrancado.
2. Corrigir F-W2-01 (ordem de colunas no `down()` da migration de `users`) — cosmético, baixo
   esforço.
3. Decidir e, se aprovado institucionalmente, construir a camada de aplicação para F-W2-02
   (rejeição de sobreposição em `ministerial_class_periods`), tratando-a com a mesma disciplina
   dos cinco skips herdados da Wave 1.
4. Decidir o destino do ficheiro Graphify órfão (F-W2-03) — mantê-lo de lado, integrá-lo
   deliberadamente numa actualização dedicada ao conteúdo P0.2, ou remover.

Nenhuma destas condições envolve alterar o esquema físico já validado da Wave 2, e nenhuma exige
recriar dados ou migrations.

---

**P0.3.2 — APROVADA PARA WAVE 3**

---

## ADENDA — P0.3.2-F Reconciliação Independente (2026-09-13)

Documento completo: `P0.3.2_wave2_independent_reconciliation.md`. Esta secção **complementa**, não
substitui, a decisão original acima; nem o texto acima nem `P0.3.2_wave2_audit.md`/
`P0.3.2_wave2_implementation.md` foram apagados ou alterados.

A condição 1 (auditoria independente) desta lista foi executada: reconciliação com ambiente
totalmente novo (contentor `mepa-wave2f-mysql`, nunca reutilizando o contentor/bases de dados da
passagem original), contagens físicas obtidas por consulta SQL directa (não apenas por
reexecução do mesmo validador), e um conjunto de cenários adversariais novos — stress de 50
workers, ligação morta a meio de transacção, corrida de 10 processos pela mesma membership,
cadeia de três transferências, transferências concorrentes incompatíveis, reutilização de
departamento nos quatro níveis territoriais, e separação estrutural Person/User/Membership. Ver
o aviso de método no topo desse documento — continua a ser a mesma sessão, não uma segunda parte
humana, mas o método foi genuinamente adversarial, não confirmatório.

**Resultado:** esta passagem encontrou um achado novo (F-W2F-01: `transfers` não tem restrição
física contra duas transferências `PENDING` concorrentes e incompatíveis para a mesma membership
— MEDIUM, mesma categoria APPLICATION_ENFORCED que F-W2-02) e reconfirmou os quatro achados
originais sem alteração. Zero CRITICAL, zero HIGH em ambas as passagens.

### Condições actualizadas a fechar durante a Wave 3

1. ~~Auditoria independente~~ — **fechada** por esta adenda (com a ressalva de método acima).
2. Corrigir F-W2-01 (ordem de colunas no `down()` de `users`) — inalterada, cosmético.
3. Decidir/construir a camada de aplicação para F-W2-02 (sobreposição ministerial) — inalterada.
4. Decidir o destino do ficheiro Graphify órfão (F-W2-03 original) — inalterada.
5. ~~Decidir/construir uma camada de aplicação que impeça duas transferências `PENDING`
   incompatíveis em simultâneo para a mesma membership (F-W2F-01)~~ — **substância resolvida**,
   ver adenda P0.3.2-M1-R abaixo (a garantia de concorrência em si está sólida; ficou uma
   sub-condição nova por fechar).

---

## ADENDA — P0.3.2-M1 e P0.3.2-M1-R (2026-09-14)

Documentos completos: `P0.3.2_M1_transfer_concurrency_fix.md` (correcção) e
`P0.3.2_M1_transfer_concurrency_audit.md` (auditoria independente da correcção).

A correcção da condição 5 acima (F-W2F-01) foi implementada — `UNIQUE` física sobre coluna
gerada `open_flag`/`closed_at` em `transfers`, mais `TransferService` com lock de âncora — e
depois auditada independentemente com ambiente totalmente novo. A auditoria **confirmou de forma
sólida e reproduzível** que a garantia central de concorrência funciona (2/10/20/30 workers,
bypass da aplicação por SQL em bruto, granularidade por membership, histórico preservado,
`origin≠destination` não mascarado, mecanismo de idempotência caracterizado com precisão) — mas
**encontrou um achado novo, HIGH**, não relacionado com a concorrência em si:

**F-M1R-01 (HIGH):** o `down()` da migration
`2026_09_14_000001_wave2m1_add_transfers_open_guard.php`, quando executado sobre uma tabela
`transfers` com histórico real, destrói irreversivelmente a distinção entre transferências
abertas e fechadas, e a reaplicação subsequente de `up()` falha. **Não afecta a operação normal
para a frente** — apenas o caminho de rollback desta migration específica sobre dados já
existentes.

### Condição nova a fechar antes de considerar F-W2F-01 totalmente encerrado

6. Corrigir F-M1R-01 (reversibilidade segura da migration M1 sobre `transfers` com histórico) e
   reauditar. Até lá: **não executar rollback desta migration em nenhum ambiente com
   transferências reais.**

O veredicto abaixo refere-se à **Wave 2 no seu conjunto** (esquema físico, `STRICT_PARITY_PASS`,
Número Único, Departamentos, etc. — nada disto foi afectado por F-M1R-01) e mantém-se válido. O
veredicto específico da tarefa de correcção M1 é separado e está registado em
`P0.3.2_M1_transfer_concurrency_audit.md`: **"P0.3.2-M1 — CORRECÇÃO NÃO APROVADA"** (apenas
quanto à reversibilidade da migration; a garantia de concorrência que motivou a correcção está
provada).

---

**P0.3.2 — APROVADA INDEPENDENTEMENTE PARA WAVE 3**


## ADENDA — P0.3.2-M1.1-R (2026-09-13)

Auditoria dedicada: [P0.3.2_M1_1_migration_safety_audit.md](P0.3.2_M1_1_migration_safety_audit.md).
A independência de ambiente e provas, e a ressalva de mesma sessão/autoria, estão declaradas no relatório.

**F-M1R-01: RESOLVED.** MySQL novo, snapshots e checksums próprios comprovam rollback do guard
sem perda de closed_at, reaplicação válida, recusa explícita de dados inválidos sem mutação,
rollback seguro da migration de domínio e concorrência M1 sem regressão. Zero CRITICAL e HIGH
pendentes no âmbito; sete suites com zero falhas e cinco skips Wave 1 preexistentes justificados.
Validadores documental e físicos: PASS. A condição 6 da adenda M1-R está encerrada.

As decisões e relatórios históricos acima ficam preservados. As condições anteriores não relacionadas
com F-M1R-01 mantêm-se. Registados um LOW documental e dois INFO no relatório da auditoria.

**Wave 2 Gate: APPROVED FOR WAVE 3.** A Wave 3 não foi iniciada por esta auditoria.
