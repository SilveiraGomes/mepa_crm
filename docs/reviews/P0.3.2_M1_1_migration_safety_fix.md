# P0.3.2-M1.1 ? Correc??o F-M1R-01: seguran?a da migration

Data: 2026-09-13. ?mbito: somente lifecycle/reversibilidade M1. Wave 3 n?o iniciada.

## Causa raiz

A migration n?o aprovada `2026_09_14_000001_wave2m1_add_transfers_open_guard.php` criou tanto
`closed_at` como `open_flag` e `uq_transfers_membership_open`. O antigo `down()` removia o ?ndice,
a coluna gerada e `closed_at`. O DROP da ?ltima coluna apagava os fechos hist?ricos. Reaplicar
criava `closed_at` com NULL para todas as linhas; duas fechadas e uma aberta tornavam-se tr?s
abertas para a mesma membership, causando duplicate-key ao reconstruir o UNIQUE.

A migration aprovada `2026_09_13_000037_wave2_create_transfers.php` n?o criou essas colunas e
n?o foi alterada. As migrations e cat?logos aprovados das Waves 1/2 permanecem intactos.

## Migration corrigida

- Nova `2026_09_14_000000_wave2m1_add_transfers_closed_at.php`: dom?nio dur?vel, ordenado depois
  de Wave 2 e antes do guard. Cria DATETIME(6) nullable apenas se ausente. Se M1 j? estiver
  aplicada, adopta a coluna sem recriar ou alterar os dados. `down()` ? intencionalmente no-op:
  o dado hist?rico permanece mesmo se o registo desta migration for revertido.
- M1 `000001` passa a possuir apenas a coluna t?cnica gerada e o UNIQUE. Exige `closed_at`
  previamente presente. `down()` remove somente o ?ndice e `open_flag`, num ALTER ?nico.
- `up()` mant?m `IF(closed_at IS NULL, 1, NULL)` e UNIQUE(membership_id, open_flag).
  A coluna gerada e o ?ndice s?o instalados juntos num ALTER MySQL at?mico.

N?o h? infer?ncia de fecho a partir de status/effective_at, nem altera??o do vocabul?rio D-11.
TransferService permanece intacto: transac??o, lock de membership e tradu??o de erros preservados.
Somente o setup dos dois testes M1 foi actualizado para aplicar a migration dur?vel antes do guard.

## Invalid data precheck

Antes de qualquer DDL t?cnico, `up()` procura memberships com COUNT(*) > 1 onde
`closed_at IS NULL`. Se houver, lan?a `TRANSFER_OPEN_GUARD_INVALID_DATA` e aborta.
N?o fecha, apaga, escolhe vencedora ou corrige dados. O teste confirma igualdade de todas as rows,
aus?ncia do guard, migration n?o registada como aplicada e zero ALTER/UPDATE/DELETE/INSERT no
query log durante a tentativa inv?lida. Se uma escrita concorrente criar duplica??o entre o
precheck e o ALTER, o UNIQUE no mesmo ALTER mant?m a defesa final sem instalar s? open_flag.

## Prova real de rollback/reapply

MySQL 8.4.11, contentor novo `mepa-m1safety-mysql`, publicado somente em 127.0.0.1:33096.
Bases sint?ticas separadas por suite; sem leitura de .env e sem acesso aos dados da aplica??o.
`WaveTwoTransferMigrationSafetyTest` usa o Migrator Laravel expl?cito; rollback de exactamente
um guard por path/step, sem depender de adivinha??o de batch.

| Estado | closed_at | open_flag | UNIQUE |
|---|---|---|---|
| A ? aplicado | presente | presente, STORED GENERATED | presente, membership_id + open_flag |
| B ? rollback guard | presente, valores iguais | ausente | ausente |
| C ? reaplicado | presente, valores iguais | presente, STORED GENERATED | presente, membership_id + open_flag |

A: `2026-02-03 04:05:06.123456`; B: `2026-03-04 05:06:07.654321`; C: NULL.
Todos os campos persistidos s?o comparados exactamente antes/depois, incluindo status,
lock_version e os restantes timestamps. Checksum SHA-256 id?ntico nas tr?s fases:
`bcb00fd5f7f904ee8093175ece9eecb599bce501e5c6f3f935fabcc43b351505`.
Depois da reaplica??o, flags A/B/C: NULL / NULL / 1. Uma quarta fechada ? aceite;
uma segunda aberta ? rejeitada com MySQL 1062. Empty DB rollback/reapply tamb?m passa.
A migration dur?vel ? revertida/reaplicada directamente e continua a preservar as mesmas rows.

Evid?ncia com snapshots reais distintos e information_schema.COLUMNS/STATISTICS:
[`m1_1_migration_safety_evidence.json`](../database/physical/m1_1_migration_safety_evidence.json).
A tentativa inv?lida posterior termina intencionalmente sem guard na base sint?tica de seguran?a;
o estado C foi capturado antes dessa prova adversarial, n?o ? confundido com o estado final dessa base.

## Regress?es

| Suite | Testes | Asser??es | FAIL | ERROR | SKIP |
|---|---:|---:|---:|---:|---:|
| WaveOnePhysicalTest | 14 | 103 | 0 | 0 | 5 |
| WaveTwoConcurrencyTest | 4 | 59 | 0 | 0 | 0 |
| WaveTwoIndependentReconciliationTest | 11 | 44 | 0 | 0 | 0 |
| WaveTwoM1IndependentAuditTest | 10 | 110 | 0 | 0 | 0 |
| WaveTwoPhysicalTest | 12 | 232 | 0 | 0 | 0 |
| WaveTwoTransferConcurrencyTest | 10 | 68 | 0 | 0 | 0 |
| WaveTwoTransferMigrationSafetyTest | 3 | 53 | 0 | 0 | 0 |

Os cinco skips Wave 1 s?o preexistentes para regras APP_ENFORCED; n?o s?o provas de enforcement f?sico.
JUnit por suite em `docs/database/physical/m1_1_*_junit.xml`.
Runner reproduz?vel: `scripts/run-m1-safety-regression.ps1`, com base vazia e prefixo expl?cito.

## Validator

Document validator: 199 tabelas, zero erros. Wave 2 static: STRICT_PARITY_PASS.
Validadores f?sicos Waves 1 e 2 executados sobre a base completa Wave 2-F, sem extens?es M1;
ambos STRICT_PARITY_PASS com zero erros. Wave 2: 74 tabelas de neg?cio inspeccionadas,
131 FKs, 57 CHECKs e zero CASCADE. Resultados finais em `m1_1_wave1_validation.json`
e `m1_1_wave2_validation.json`.
As extens?es M1 s?o verificadas pelo teste real de information_schema nos estados A/B/C.
N?o se alterou o cat?logo Wave 2 para ocultar a extens?o M1.

Nota de execu??o: a tentativa est?tica Wave 1 sem --allow-deferred mostrou o diferimento hist?rico
W1-F01; uma tentativa f?sica depois do teardown Wave 1 encontrou a base j? desmontada.
Ambas foram substitu?das por valida??o f?sica na base completa Wave 2, com W1-F01 materializado.
Esses diagn?sticos n?o s?o classificados como novas falhas de regress?o nem omitidos.

## Graphify

Consultado antes de editar: 2301 n?s; consulta BFS com vocabul?rio real
[transfer, transfers, migration, rollback, closed, open, guard, concurrency]. Localizou a migration,
closed_at/open_flag, o UNIQUE e as fontes hist?ricas. Consulta limitada a 1500 tokens reportou
truncamento expl?cito; o c?digo e as auditorias foram tamb?m lidos directamente.
Actualiza??o de c?digo via AST e extrac??o sem?ntica local limitada a este relat?rio; preserva??o
dos n?s dos relat?rios hist?ricos. Shrink guard usa `to_json(... force=False)`.
Grafo: 2301 ? 2329 n?s; 3876 ? 3944 arestas. Shrink guard PASS, sem force.
Sa?de: zero endpoints ausentes/soltos, duplicados e self-loops. A primeira extrac??o
assinalou oito arestas sem source_file; a proveni?ncia foi preenchida antes da execu??o final.
Contagem de tokens da extrac??o local ? estimada por caracteres/4.
Evid?ncia final: `m1_1_graphify_validation.json`; script `scripts/update-m1-safety-graph.py`.

## Gate

F-M1R-01: RESOLVED por prova real MySQL de preserva??o, rollback, reaplica??o e precheck,
com zero falhas nas regress?es. Pronto para auditoria; n?o representa aprova??o de produ??o.

**P0.3.2-M1.1 ? F-M1R-01 RESOLVIDO E PRONTO PARA AUDITORIA**

## Adenda P0.3.3 — errata de legibilidade

O texto histórico acima é preservado. Os caracteres substituídos por `?` têm leitura corrigida na [adenda de legibilidade](P0.3.3_previous_findings_addendum.md). Não houve alteração dos resultados, SQL, dados históricos ou resolução F-M1R-01. Os novos documentos Wave 3 são UTF-8.
